import React from 'react';
import './Navbar.css';

const AuthButton = () => {
    return (
        <button className="auth-button">
            Join Now
        </button>
    );
};

export default AuthButton;
